<h1>Posts</h1>

<table class="table">
    <tr>
        <th>Título</th>
        <th>Fecha</th>
        <th>Imagen</th>
        <th>Contenido</th>
    </tr>
    <% @posts.each do |post| %>
    <tr>                       
        <td><%= post.title %></td>
        <td><%= post.created_at %></td>
        <td><%=image_tag post.image_url%></td>
        <td><%= post.content %></td>
    </tr>
    <% end %>
</table>